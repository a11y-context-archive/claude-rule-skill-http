---
description: Invoke the a11y-context-reactjs skill before generating front-end React code.
alwaysApply: true
---

# Accessibility Policy

Before generating front-end, user-facing React code, run the `a11y-context-reactjs` skill.

This applies whenever the task involves creating or modifying React components, pages, or UI elements that a user will interact with or see.
